---
title: "geom_seg"
format: 
  dashboard:
    scrolling: false
server: shiny
---
```{r}
#| label: setup
# Load packages and functions
library(shiny)
library(quarto)
library(ggplot2)
library(plotly)
```

# Tab 1
```{r}
#| title: "Diagnostic Plot for K/Ti"
plotlyOutput("plot1", height = "100%", width = "100%")
```


```{r}
#| context: server

df <- data.frame(x = 1:3, y = c(4, 4, 4))
b <- ggplot(df, aes(x, y))+ geom_path(linewidth = 5)

output$plot1 <- renderPlotly({
  ggplotly(b)
})
```